import { createTheme } from "@mui/material/styles";

export const muiTheme = createTheme({
  palette: {
    primary: {
      main: "#417ee3",
    },
    success: {
      main: "#74C05A",
    },
  },
  shape: {
    borderRadius: 4,
  },
});
