# Paste into Flywheel (or nginx on origin) so clients with OLD cached DNS (151.101.x) still reach /resources.
# Requests arrive with Host: smoothhiring.com and URI /resources/...
location ~* ^/resources {
  return 301 https://smoothhiring.com$request_uri;
}
